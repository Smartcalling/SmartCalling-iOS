platform :ios, '10.0'
project 'DemoApp/DemoApp.xcproj'
use_frameworks!

target 'DemoApp' do
  pod 'SmartCallingSDK', :path =>  './'
  pod 'Firebase/Analytics'
  pod 'Firebase/Messaging'
end

target 'CallKitExtension' do
  pod 'SmartCallingSDK', :path =>  './'
end
